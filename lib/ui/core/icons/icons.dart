import 'package:flutter/material.dart';

abstract class Icons {
  static const _fontFamily =
      'iuxIconsFont'; // Must match the one in pubspec.yaml

  static const plus = IconData(0xe000, fontFamily: _fontFamily);
}
